# TODO

- Replace coordinate-based prompt targeting with a reliable accessibility or app-level injection path if Codex Desktop exposes one.
- Detect or support the Codex Desktop new-conversation start screen, where the prompt input can be centered instead of near the bottom.
- Add a packaged Windows launcher if Q-Link graduates beyond a local script/tray helper.
